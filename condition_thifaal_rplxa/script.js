document.getElementById("orderButton").onclick = fuction () {
    let foodPrice = parseInt(document.getElementById ("food").value);
    let drinkPrice = parseInt(document.getElementById ("drink").value);

    let foodPrice = foodPrice + drinkPrice;

    let discount = 0;

    if (totalPrice > 100) {
        alert(('ok'))
    }

}